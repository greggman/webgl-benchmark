{
  "name": "webgl-benchmark",
  "version": "1.0.0",
  "description": "A browser benchmark suite that measures the performance of the WebGL implementation (API/driver overhead), not the GPU.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/dev.mjs",
    "serve": "node scripts/serve.mjs",
    "test": "node --test 'test/*.test.mjs'",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "fix": "eslint . --fix",
    "baseline": "node test/baseline.mjs",
    "counts": "node test/gen-counts.mjs"
  },
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.28.1",
    "globals": "^17.6.0",
    "gts": "^7.0.0",
    "puppeteer": "^25.1.0",
    "typescript": "^6.0.3"
  }
}
